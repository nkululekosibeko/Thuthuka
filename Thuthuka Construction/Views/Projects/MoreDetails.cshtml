@* @model Thuthuka_Construction.Models.CustomerProject

<div class="container my-5">
    <h2 class="text-center mb-4">Project Details</h2>

    <div class="card shadow-sm">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-4">Project Name</dt>
                <dd class="col-sm-8">@Model.ProjectName</dd>

                <dt class="col-sm-4">Description</dt>
                <dd class="col-sm-8">@Model.Description</dd>

                <dt class="col-sm-4">Status</dt>
                <dd class="col-sm-8">@Model.Status</dd>

                <dt class="col-sm-4">Reason</dt>
                <dd class="col-sm-8">@Model.Reason</dd>

                <dt class="col-sm-4">Start Date</dt>
                <dd class="col-sm-8">@Model.StartDate.ToShortDateString()</dd>

                <dt class="col-sm-4">End Date</dt>
                <dd class="col-sm-8">@Model.EndDate?.ToShortDateString()</dd>
            </dl>
        </div>
    </div>

    <div class="mt-4">
        <a class="btn btn-primary" asp-area="" asp-controller="CustomerProjects" asp-action="Index">Back to Projects List</a>
    </div>
</div>
 *@