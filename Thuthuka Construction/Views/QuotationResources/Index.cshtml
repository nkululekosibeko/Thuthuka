@model IEnumerable<Thuthuka_Construction.Models.QuotationResource>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quotation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Resource)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quotation.QuotationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Resource.ResourceId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QuotationResourceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QuotationResourceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QuotationResourceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
