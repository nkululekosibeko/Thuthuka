@* @model Thuthuka_Construction.Models.CustomerProject

@{
    ViewData["Title"] = "Delete Customer Project";
}

<div class="card shadow border-0 my-4">
    <div class="card-header bg-danger bg-gradient py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Delete Customer Project</h2>
            </div>
        </div>
    </div>

    <div class="card-body p-4">
        <h3 class="text-danger text-center">Are you sure you want to delete this project?</h3>
        <div class="border p-3">
            <dl class="row">
                <!-- Customer Name -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Customer)
                </dt>
                <dd class="col-sm-9">
                    <div class="form-floating">
                        <input class="form-control border-0 shadow" value="@Model.Customer.Name" readonly />
                        <label>@Html.DisplayNameFor(model => model.Customer)</label>
                    </div>
                </dd>

                <!-- Select Date -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.SelectDate)
                </dt>
                <dd class="col-sm-9">
                    <div class="form-floating">
                        <input class="form-control border-0 shadow" value="@Model.SelectDate.ToString("yyyy-MM-dd")" readonly />
                        <label>@Html.DisplayNameFor(model => model.SelectDate)</label>
                    </div>
                </dd>

                <!-- Status -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Status)
                </dt>
                <dd class="col-sm-9">
                    <div class="form-floating">
                        <input class="form-control border-0 shadow" value="@Model.Status" readonly />
                        <label>@Html.DisplayNameFor(model => model.Status)</label>
                    </div>
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="CustomerProjectId" />
                <div class="row pt-2">
                    <div class="col-6 col-md-3">
                        <input type="submit" value="Delete" class="btn btn-danger form-control" />
                    </div>
                    <div class="col-6 col-md-3">
                        <a asp-action="Index" class="btn btn-outline-primary border form-control">Back to List</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.RenderPartialAsync("_ValidationScriptsPartial")
}
 *@